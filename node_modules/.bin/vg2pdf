#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../vega-cli/bin/vg2pdf" "$@"
  ret=$?
else 
  node  "$basedir/../vega-cli/bin/vg2pdf" "$@"
  ret=$?
fi
exit $ret
